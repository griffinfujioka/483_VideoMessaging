<UserControl x:Class="_483_VideoMessaging_WP7.User_Controls.VideoEntryControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="480" d:DesignWidth="480"
             Height="360"
             Width="360">
    
    <Grid x:Name="LayoutRoot" Background="{StaticResource PhoneChromeBrush}">

        <StackPanel Orientation="Vertical">
            <Image x:Name="videoThumbnail" Height="180" Width="180" Margin="40,20,10,20" 
                   Source="{Binding ThumbnailImage}" HorizontalAlignment="Left"/>
            <TextBlock x:Name="titleTxtBlock" Height="40" Width="270" Text="{Binding Title}"/>
            
        </StackPanel>
    </Grid>
</UserControl>